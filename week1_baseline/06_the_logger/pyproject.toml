[project]
name = "boukensha-step-06-the-logger"
version = "0.1.0"
description = "Python-Port fuer Boukensha Woche 1 Schritt 06 (The Logger)."
requires-python = ">=3.11"
dependencies = [
  "python-dotenv>=1.0",
  "PyYAML>=6.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

