[project]
name = "boukensha-step-04-api-client"
version = "0.1.0"
description = "Python-Port fuer Boukensha Woche 1 Schritt 04 (API Client)."
requires-python = ">=3.11"
dependencies = [
  "python-dotenv>=1.0",
  "PyYAML>=6.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

