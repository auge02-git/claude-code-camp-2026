[project]
name = "boukensha-step-08-repl-loop"
version = "0.8.0"
description = "Python-Port fuer Boukensha Woche 1 Schritt 08 (REPL Loop)."
requires-python = ">=3.11"
dependencies = [
  "python-dotenv>=1.0",
  "PyYAML>=6.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

